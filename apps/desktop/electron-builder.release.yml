extends: ./electron-builder.yml
artifactName: '${productName}-${version}-${arch}.${ext}'
forceCodeSigning: true
extraMetadata:
  consolaAutoUpdates: true
publish:
  provider: github
  owner: ConsolaDev
  repo: Consola
  releaseType: draft
mac:
  hardenedRuntime: true
  notarize: true
  target:
    - target: dmg
      arch: [arm64, x64]
    - target: zip
      arch: [arm64, x64]
